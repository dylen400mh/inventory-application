<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Items</title>
  </head>
  <body>
    <h1>Items</h1>
    <a href="/items/department/<%=department_id%>/create">Create Item</a>
    <ul>
      <% items.forEach(item => { %>
      <li>
        <h2><%=item.name %></h2>
        <a href="/items/update/<%=item.id %>">(Update)</a>
        <p>Price: <%=item.price %></p>
        <p>Quantity: <%=item.quantity %></p>
        <p>Description: <%=item.description %></p>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
